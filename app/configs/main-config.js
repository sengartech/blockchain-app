/**
 * setting common configs.
 */
const commonConfig = {
  name: 'blockchain app',
  description: 'This is a digital, decentralized, distributed ledger system.',
  version: '1.0.0',
  port: 3000
} // end of commonConfig.

// setting commonConfig in global variable.
global.config = commonConfig
